# Online Notes API

Express + MongoDB API for the Online Notes App.

## Run
1. Install Node.js 18+ and MongoDB.
2. Copy `.env.example` to `.env` and set `MONGO_URI` and `JWT_SECRET`.
3. `npm install`
4. `npm run dev`

API: `http://localhost:5000`
